{
  "name": "chrome-extension-sa-publish-action",
  "version": "1.0.0",
  "description": "Publish a chrome extnsion to google webstore using a service account",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "pack": "ncc build bin --license licenses.txt",
    "build:pack": "npm run build && npm run pack"
  },
  "author": "V-Launcher",
  "license": "ISC",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0",
    "jsonwebtoken": "^9.0.2",
    "superagent": "^10.2.0"
  },
  "devDependencies": {
    "@types/jsonwebtoken": "^9.0.9",
    "@types/superagent": "^8.1.9",
    "typescript": "^5.8.2"
  }
}
